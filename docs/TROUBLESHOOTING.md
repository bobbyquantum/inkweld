# Troubleshooting (moved)

Cookie-related production fixes are documented inside the Docusaurus site: <https://docs.inkweld.org/docs/troubleshooting/cookies> (source: `docs/site/docs/troubleshooting/cookies.md`).

Preview the docs locally:

```bash
cd docs/site
npm install
npm run start
```
