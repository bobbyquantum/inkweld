[install]
# Prefer Bun's native package manager
prefer = "online"

[runtime]
# Enable TypeScript support
typescript = true

[test]
# Use Bun's native test runner
timeout = 5000
# Include e2e test files
include = ["**/*.spec.ts", "**/*.e2e-spec.ts"]
