{
  "name": "worm-project",
  "version": "1.0.0",
  "description": "Worm project",
  "scripts": {
    "install-all": "bun install && cd frontend && npm install && cd ../worm-server && bun install",
    "lint:ci": "bun --cwd frontend run lint && bun --cwd worm-server run lint",
    "testTool": "bun --cwd frontend run test && bun --cwd worm-server run test"
  },
  "devDependencies": {
  },
  "packageManager": "bun@1.1.42"
}
