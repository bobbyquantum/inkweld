{
  "name": "worm-project",
  "version": "1.0.0",
  "description": "Worm project with frontend, backend, and api-client",
  "scripts": {
    "update-api-client": "node update-api-client.js",
    "start-frontend": "cd frontend && npm start",
    "start-backend": "cd backend && .\\mvnw.cmd spring-boot:run",
    "start": "npm-run-all --parallel start-frontend start-backend",
    "install-all": "npm install && cd frontend && npm install && cd ../api-client && npm install",
    "build": "npm run update-api-client && cd frontend && npm run build && cd ../backend && .\\mvnw.cmd clean package"
  },
  "devDependencies": {
    "npm-run-all": "^4.1.5"
  }
}