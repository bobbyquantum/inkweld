{
  "name": "worm-project",
  "version": "1.0.0",
  "description": "Worm project",
  "scripts": {
    "install-all": "bun install && cd frontend && bun install && cd ../worm-server && bun install",
    "lint": "bun --cwd frontend lint && bun --cwd worm-server lint",
    "test": "bun --cwd frontend test && bun --cwd worm-server test"
  },
  "devDependencies": {
  },
  "packageManager": "bun@1.1.42"
}
