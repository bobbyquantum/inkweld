{
  "name": "worm-project",
  "version": "1.0.0",
  "description": "Worm project",
  "scripts": {
    "install-all": "bun install && cd frontend && npm install && cd ../worm-server && bun install",
    "lint": "cd frontend && npm run lint && cd ../worm-server && bun run lint",
    "test": "cd frontend && npm test && cd ../worm-server && bun test"
  },
  "devDependencies": {
  },
  "packageManager": "bun@1.1.42"
}
